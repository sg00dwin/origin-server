#!/bin/bash

source "/etc/openshift/node.conf"
source ${CARTRIDGE_BASE_PATH}/abstract/info/lib/util
application="$1"
uuid=$2
src=$3
dest=$4
setup_basic_vars
setup_user_vars

rm -f $dest
ln -s $src $dest
chcon -u system_u -t openshift_var_lib_t -l $mcs_level --no-dereference $dest