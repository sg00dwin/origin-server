$(function(){var navigation,menus,labels,menu_widths;navigation=$("#nav");menus=$("ul",navigation);labels=$(".category",navigation);menu_widths={};menus.each(function(){menu_widths[$(this).attr("id")]=$(this).width();$(this).css("height",$(this).height());if($(this).hasClass("collapsed")){$(this).css({width:0})}});labels.click(function(){var id,menu;id=$(this).attr("data-category");menu=menus.filter("#"+id);if(menu.hasClass("collapsed")){menus.each(function(){var label=labels.filter('[data-category="'+$(this).attr("id")+'"]');if($(this)!==menu&&$(this).hasClass("expanded")){collapseMenu($(this),label)}});expandMenu(menu,$(this))}else{collapseMenu(menu,$(this))}});function collapseMenu(menu,label){menu.removeClass("expanded").addClass("collapsed");menu.stop().animate({width:0},300,function(){label.removeClass("expanded").addClass("collapsed")})}function expandMenu(menu,label){var menu_width=menu_widths[menu.attr("id")];label.removeClass("collapsed").addClass("expanded");menu.removeClass("collapsed").addClass("expanded");menu.stop().animate({width:menu_width},300)}});