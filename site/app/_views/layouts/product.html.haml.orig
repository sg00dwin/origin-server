- content_for :sub_layout do
  = yield :pageheader

  #page.container_24
    / Main page content
    #main_content.grid_17
      /About the product
      #product_information.grid_8
        = yield :about
        
      #product_screenshot.grid_8
        = yield :screenshot
      
      .clear
          
      / Product instructions
      #product_tabs.grid_16
        = yield :tabs
      
          
      / Videos
      #product_videos.grid_8
        %h2 Videos
        = yield :videos

      / Featured community content
      #product_community.grid_8
        %h2 Community Highlights
        = yield :community
        
      .clear
        
    
    / Sidebar
    %div{:id => 'supplemental_content',
         :class => params[:action] + ' grid_6' }
      
      /Try it button
<<<<<<< HEAD
      = yield :try_it_button
=======
      - if session[:login]
        - if session[:user].has_access?(CloudAccess.access_id(params[:action]))
          = link_to 'Get Started!', 'getting_started/' + params[:action], :class => 'try_it'
        - else# session[:user].has_requested?(CloudAccess.access_id(params[:action]))
          = link_to 'Try it now!', 'access/' + params[:action] + '/new', :class => 'try_it'
      - else
        = link_to 'Try it now!', 'user/new/' + params[:action], :class => 'try_it'
>>>>>>> 792e55e0f6fcd7247dd6f3d1233db906fe8d0557
      
      /Resource links
      #resources
        %h3 Resources
        = yield :resources
        
      /Tour box
      #tour
        = yield :tour_link
      
      /Platforms
      #platforms
        %h3 Supported Platforms
        = yield :platforms
      
      /Top video
      #top_video
        %h3 Today's Top Video
        = yield :top_video
            
      /Promos
      = yield :promos

    .clear
    
= render :file => 'layouts/application'
