- content_for :page_title, 'Account Help'
-#.row
  %section
    .navbar
      = render :partial => 'help_banner', :locals => {:user => @user, :user_on_basic_plan => @user_on_basic_plan}

= flashes
.row
  .span8
    %section
      = render :partial => 'faq', :locals => {:topten => @topten}
  .span4
    %aside
      -if @user_on_basic_plan
        = render :partial => 'why_upgrade'
      .sidebar
        = render :partial => 'customer_service'
        = render :partial => 'contact', :locals => {:user => @user, :user_on_basic_plan => @user_on_basic_plan}
      .sidebar
        = render :partial => 'tech_support'

