
build: 
  Build Artifacts
client:
  Contains the client tools
common:
  Artifacts shared across server and node (and potentially client)
misc:
  Bucket for documentation/development utilities/etc
node:
  Contains code specific to the nodes which run the various cartridges
site:
  Contains site
broker:
  Contains broker
server-common:
  Contains code common to site and broker
tests:
  Location for cucumber test cases